{% extends 'base.html.twig' %}

{% block body %}
    <h1>CtlTipoEmpresa</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ctlTipoEmpresa.id }}</td>
            </tr>
            <tr>
                <th>Tipoempresa</th>
                <td>{{ ctlTipoEmpresa.tipoEmpresa }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_tipoempresa_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_tipoempresa_edit', { 'id': ctlTipoEmpresa.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
