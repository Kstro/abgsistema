{% extends 'base.html.twig' %}

{% block body %}
    <h1>AdmImagenPromocion</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admImagenPromocion.id }}</td>
            </tr>
            <tr>
                <th>Imagen</th>
                <td>{{ admImagenPromocion.imagen }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_imagenpromocion_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_imagenpromocion_edit', { 'id': admImagenPromocion.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
